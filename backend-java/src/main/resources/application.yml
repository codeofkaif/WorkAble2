server:
  port: ${PORT:5001}

spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:ai_job_accessibility}
    username: ${POSTGRES_USER:kaifkhan}
    password: ${POSTGRES_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://kaif6388:KAIFkhan6388@cluster0.rxnir.mongodb.net/ai-job-accessibility?retryWrites=true&w=majority&appName=Cluster0}
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB

app:
  jwt:
    secret: Kaifkhan638891@&
  gemini:
    api-key: ${GEMINI_API_KEY:AIzaSyClsioS040Ydo1xE35FHvssQbN24CNr9f8}
  context-engine:
    url: ${CONTEXT_ENGINE_URL:http://127.0.0.1:8000}
    api-key: ${CONTEXT_ENGINE_API_KEY:test-api-key}
    knowledge-base-id: ${KNOWLEDGE_BASE_ID:kb-workable-001}
  env: ${SPRING_PROFILES_ACTIVE:development}
  # External Job Provider API Keys
  # Set these as environment variables (or update values here for local dev).
  # Each provider skips gracefully if its key is blank.
  jobs:
    jooble:
      api-key: ${JOOBLE_API_KEY:}
    adzuna:
      app-id: ${ADZUNA_APP_ID:}
      api-key: ${ADZUNA_API_KEY:}
      country: ${ADZUNA_COUNTRY:us}
    jsearch:
      api-key: ${JSEARCH_API_KEY:}
      host: ${JSEARCH_HOST:jsearch.p.rapidapi.com}

logging:
  level:
    com.ai.accessibility: DEBUG
    org.springframework.web: INFO
